const $=(q)=>document.querySelector(q);let lang=localStorage.getItem('lang')||'en';const site=(PROFILE.website||location.href).trim();function setLinks(){creatorsLink.href=PROFILE.links.creators||'#';paypalLink.href=PROFILE.links.paypal||'#';tiktokLink.href=PROFILE.links.tiktok||'#';telegramLink.href=PROFILE.links.telegram||'#';qrImage.src='https://api.qrserver.com/v1/create-qr-code/?size=180x180&margin=10&data='+encodeURIComponent(site);year.textContent=new Date().getFullYear()}function applyLang(l){lang=l;localStorage.setItem('lang',l);document.documentElement.lang=l;document.documentElement.dir=l==='ar'?'rtl':'ltr';document.querySelectorAll('[data-i18n]').forEach(el=>{el.textContent=PROFILE.labels[l][el.dataset.i18n]||el.textContent});document.querySelectorAll('.lang-switch button').forEach(b=>b.classList.toggle('active',b.dataset.lang===l))}function toast(t){const x=$('#toast');x.textContent=t;x.classList.add('show');setTimeout(()=>x.classList.remove('show'),1700)}async function copyLink(){try{await navigator.clipboard.writeText(site);toast(PROFILE.labels[lang].copied)}catch(e){toast(site)}}async function sharePage(){if(navigator.share){try{await navigator.share({title:PROFILE.name,text:PROFILE.bio,url:site})}catch(e){}}else copyLink()}function toggleTheme(){document.body.classList.toggle('light');localStorage.setItem('theme',document.body.classList.contains('light')?'light':'dark')}setLinks();applyLang(lang);if(localStorage.getItem('theme')==='light')document.body.classList.add('light');document.querySelectorAll('.lang-switch button').forEach(b=>b.onclick=()=>applyLang(b.dataset.lang));langBtn.onclick=()=>applyLang(lang==='en'?'ar':'en');copyBtn.onclick=copyLink;shareBtn.onclick=sharePage;themeBtn.onclick=toggleTheme;themeTop.onclick=toggleTheme;qrBtn.onclick=()=>window.open(qrImage.src,'_blank');window.addEventListener('load',()=>setTimeout(()=>loader.classList.add('hide'),650));if('serviceWorker' in navigator){window.addEventListener('load',()=>navigator.serviceWorker.register('sw.js').catch(()=>{}))}
const canvas=$('#bgCanvas'),ctx=canvas.getContext('2d');let w,h,t=0,particles=[];function resize(){w=canvas.width=innerWidth*devicePixelRatio;h=canvas.height=innerHeight*devicePixelRatio;canvas.style.width=innerWidth+'px';canvas.style.height=innerHeight+'px';particles=Array.from({length:Math.min(110,Math.floor(innerWidth/8))},()=>({x:Math.random()*w,y:Math.random()*h,r:Math.random()*2+0.4,v:Math.random()*0.4+0.1,a:Math.random()*6.28}))}addEventListener('resize',resize);resize();function draw(){t+=0.008;ctx.clearRect(0,0,w,h);let g=ctx.createRadialGradient(w/2,h*.25,0,w/2,h*.25,w*.65);g.addColorStop(0,'rgba(255,0,0,.18)');g.addColorStop(1,'rgba(0,0,0,0)');ctx.fillStyle=g;ctx.fillRect(0,0,w,h);ctx.strokeStyle='rgba(255,0,0,.45)';ctx.lineWidth=1*devicePixelRatio;for(let side of [-1,1]){for(let row=0;row<18;row++){ctx.beginPath();for(let i=0;i<95;i++){let x=(i/94)*w;let base=h*.33+row*10*devicePixelRatio;let y=base+Math.sin(i*.17+t*2+row*.33)*45*devicePixelRatio;let edge=side<0?x<w*.5:x>w*.5;if(edge){if(i===0)ctx.moveTo(x,y);else ctx.lineTo(x,y)}}ctx.stroke()}}particles.forEach(p=>{p.x+=Math.cos(p.a)*p.v*devicePixelRatio;p.y+=Math.sin(p.a)*p.v*devicePixelRatio;if(p.x<0)p.x=w;if(p.x>w)p.x=0;if(p.y<0)p.y=h;if(p.y>h)p.y=0;ctx.beginPath();ctx.fillStyle='rgba(255,35,35,.65)';ctx.arc(p.x,p.y,p.r*devicePixelRatio,0,6.28);ctx.fill()});requestAnimationFrame(draw)}draw();